{% extends "bases.html.twig" %}

{% block main %}

	<h1>{{ category.name }}</h1>

	<h2>{{ category.description }}</h2>

	{% for post in category.posts %}
		<h2>{{ post.title }}</h2>
	{% endfor %}

{% endblock %}
